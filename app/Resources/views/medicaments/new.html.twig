{% extends 'base.html.twig' %}
{% block page_title %}Nouvel médicament{% endblock %}
{% block body %}
    <h1>Medicament creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('fournisseur_medicaments_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
